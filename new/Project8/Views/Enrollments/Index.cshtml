@model IEnumerable<Project8.Models.Enrollment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Is_Paid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Course_mark)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUser.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Courses_Offered.Hall)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.semester.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Is_Paid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Course_mark)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Courses_Offered.Hall)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.semester.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Enrollment_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Enrollment_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Enrollment_Id })
        </td>
    </tr>
}

</table>
